{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "chat",
  "description": "Chat: HTTP requests to the LLM and TTS (base URL is configurable — any address is allowed), and hiding its own window.",
  "windows": ["chat"],
  "permissions": [
    "core:window:allow-hide",
    {
      "identifier": "http:default",
      "allow": [{ "url": "http://**" }, { "url": "https://**" }]
    }
  ]
}
